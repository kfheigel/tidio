{% extends 'base.html.twig' %}

{% block title %}Add Department{% endblock %}

{% block body %}
    <div class="example-wrapper">
        {% for message in app.flashes('success') %}
            <div class="alert alert-success">
                {{ message }}
            </div>
        {% endfor %}
        <h1>Add new department</h1>
        {{ form_start(form) }}
            {{ form_widget(form) }}

        <input class="button" style="margin:10px 0; background-color: #04AA6D;" type="submit">
        {{ form_end(form) }}
        <a href="{{ path('payroll') }}"><button style="background-color: #3300cc;" class="button">Go To Payroll</button></a>
        <a href="{{ path('employee') }}"><button style="background-color: #04AA6D;" class="button">Add Employee</button></a>
    </div>
{% endblock %}





